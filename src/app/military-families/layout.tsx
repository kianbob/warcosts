import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Military Families — The Hidden Cost of War | WarCosts',
  description: 'Divorce, PTSD, deployment cycles, and child impact. The toll of war on the families who serve alongside soldiers.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
