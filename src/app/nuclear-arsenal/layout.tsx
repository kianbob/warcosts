import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'US Nuclear Arsenal — 5,500 Warheads, $1.7T Modernization | WarCosts',
  description: '5,500 warheads, $60.7B annual budget, $1.7 trillion modernization plan. The full scope of America\'s nuclear arsenal.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
